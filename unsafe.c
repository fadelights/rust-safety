#include <stdio.h>

void transactions(int* x) {
    // read some transactions from a user account
}

int main() {
    int x[5];

    transactions(x);

    for (int i = 0; i < 5; i++) {
        printf("%d\n", x[i]);
    }
}
